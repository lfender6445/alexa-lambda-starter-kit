{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "scripts": {
    "zip": "rm -f ./Archive.zip; zip -r ./Archive.zip *",
    "upload": "aws lambda update-function-code --function-name helloGoodbye --zip-file fileb://Archive.zip",
    "lambda": "npm run zip && npm run upload"
  },
}
